Future<String> savePayrollFile({
  required List<int> bytes,
  required String fileName,
  required String mimeType,
}) async {
  throw UnsupportedError('Le telechargement de fichier n est pas disponible sur cette plateforme.');
}
